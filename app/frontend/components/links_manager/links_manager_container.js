import { connect } from 'react-redux';
import { LinksManager } from './links_manager';

const mapStateToProps = () => ({
});

const mapDispatchToProps = dispatch => ({
});

export default connect(
  mapStateToProps,
  mapDispatchToProps
)(LinksManager);
